# Backend origin used in production builds, e.g. https://api.example.com
# Leave empty for local development — requests go to the relative `/api`
# path which Vite proxies to the Django dev server (see vite.config.ts).
VITE_API_BASE=
